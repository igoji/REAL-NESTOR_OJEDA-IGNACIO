DROP TABLE IF EXISTS nombreTabla; CREATE TABLE nombreTabla(ID INT AUTO_INCREMENT PRIMARY KEY, codigo INT NOT NULL, nombre VARCHAR(250) NOT NULL, apellido VARCHAR(250) NOT NULL, edad INT NOT NULL, precio NUMERIC(10, 2) NOT NULL);